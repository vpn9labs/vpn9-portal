<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h2 class="text-2xl font-bold text-gray-900 mb-6">Marketing Banners</h2>

  <div class="bg-white rounded-lg shadow p-6 mb-6">
    <p class="text-gray-600 mb-4">Copy the HTML code below each banner to add it to your website</p>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <% @banners.each do |banner| %>
      <div class="bg-white rounded-lg shadow p-6">
        <h3 class="text-lg font-semibold mb-2"><%= banner[:name] %></h3>
        <p class="text-sm text-gray-500 mb-4"><%= banner[:size] %></p>
        
        <div class="border rounded p-4 bg-gray-50 mb-4">
          <img src="/images/<%= banner[:image] %>" alt="<%= banner[:name] %>" class="max-w-full h-auto">
        </div>
        
        <div class="mb-4">
          <label class="block text-sm font-medium text-gray-700 mb-2">HTML Code:</label>
          <div class="relative">
            <textarea readonly class="w-full p-3 text-xs font-mono bg-gray-100 rounded border border-gray-300" rows="4"><a href="<%= root_url(ref: @current_affiliate.code) %>"><img src="<%= root_url %>images/<%= banner[:image] %>" alt="VPN9 - Secure Your Privacy" width="<%= banner[:size].split('x')[0] %>" height="<%= banner[:size].split('x')[1] %>"></a></textarea>
            <button onclick="copyToClipboard(this.parentElement.querySelector('textarea').value)" class="absolute top-2 right-2 px-3 py-1 text-xs bg-indigo-600 text-white rounded hover:bg-indigo-700">
              Copy
            </button>
          </div>
        </div>
        
        <div class="flex space-x-3">
          <a href="/images/<%= banner[:image] %>" download class="px-4 py-2 bg-gray-600 text-white rounded hover:bg-gray-700">
            Download
          </a>
        </div>
      </div>
    <% end %>
  </div>

  <div class="bg-white rounded-lg shadow p-6 mt-6">
    <h3 class="text-lg font-semibold mb-4">Implementation Tips</h3>
    <ul class="space-y-2 text-gray-600">
      <li>• Place banners above the fold for maximum visibility</li>
      <li>• Use responsive banners that adapt to different screen sizes</li>
      <li>• Test different banner positions to find what works best</li>
      <li>• Match banner style with your website design</li>
      <li>• Monitor performance and adjust placement accordingly</li>
    </ul>
  </div>
</div>

<script>
function copyToClipboard(text) {
  navigator.clipboard.writeText(text).then(function() {
    alert('Copied!');
  }, function(err) {
    console.error('Could not copy text: ', err);
  });
}
</script>